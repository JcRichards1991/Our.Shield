# ASP.NET
# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: 'src/Our.Shield.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:PackageLocation="$(build.artifactStagingDirectory)"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

#- task: VSTest@2
#  inputs:
#    platform: '$(buildPlatform)'
#    configuration: '$(buildConfiguration)'
    
- task: NuGetCommand@2
  inputs:
    command: 'pack'
    packagesToPack: 'src/**/*.nupsec'
    versioningScheme: 'off'
    verbosityPack: 'Normal'
    packDestination: '$(Build.ArtifactStagingDirectory)'

- task: AzureFileCopy@4
  inputs:
    SourcePath: '$(Build.ArtifactStagingDirectory)'
    azureSubscription: 'Pay-As-You-Go(a3138c5a-b600-4ff1-b46d-53ab2efe667f)'
    Destination: 'AzureVMs'
    storage: 'Shield'
    resourceGroup: 'VAINRADICAL-RADZYWRITES'
    vmsAdminUserName: 'joshua.richards'
    vmsAdminPassword: 'C0mpK1dW!z1991'
    TargetPath: 'C:\Shield'
